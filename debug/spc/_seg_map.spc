%;; Copyright	1984, 1989	Massachusetts Institute of Technology

_seg_map = cluster is
	get_owns,
	get_wvecs

	rep = _vec[_obj]

	get_owns = proc (sm: cvt) returns (_obj, int)
		end get_owns

	get_wvecs = proc (sm: cvt) returns (_obj, int)
		end get_wvecs

	end _seg_map
